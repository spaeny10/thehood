{
    "name": "kanopolanes",
    "version": "1.0.0",
    "description": "Kanopolanes Weather2000 Dashboard",
    "private": true,
    "scripts": {
        "build": "cd backend && npm install && cd ../frontend && npm install && npm run build",
        "start": "cd backend && node src/server.js"
    },
    "engines": {
        "node": ">=18.0.0"
    }
}